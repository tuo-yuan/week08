# Deliberate business-route regression: Kubernetes readiness still passes.
server {
    listen 80;
    root /usr/share/nginx/html;
    location = /healthz { return 200 'ready\n'; }
    location = /release.txt { }
    location / { return 500 'Injected 10.3HD release regression\n'; }
}
