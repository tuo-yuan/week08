# Isolated frontend-only fixture. Backend API integration is outside this demo.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location = /healthz { return 200 'ready\n'; }
    location / { try_files $uri $uri/ /index.html; }
}
